{
  "name": "portfolio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build --turbopack  && next export",
    "build:gh": "GITHUB_PAGES=true next build && next export",
    "start": "next start",
    "lint": "eslint"
  },
  {
  "scripts": {
    "build": "next build && next export",
    "build:gh": "GITHUB_PAGES=true next build && next export",
    "dev": "next dev"
  }
}

  "dependencies": {
    "react": "19.1.0",
    "react-dom": "19.1.0",
    "next": "15.5.2"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "tailwindcss": "^4",
    "eslint": "^9",
    "eslint-config-next": "15.5.2",
    "@eslint/eslintrc": "^3"
  }
}
